/**
 * Custom script for component
 */
(function($){

	var plugin = {};
	plugin.el = $('.my-component'); // Set top-level component element scope

	if (plugin.el.length === 0) {
		return false;
	}

	// Plugin custom code here

})(jQuery);
